Import('env')
Import('base')
Import('domain')
Import('jsoncpp')
Import('zmq')

prog = env.Program(target='nitro', source=Glob('*.cpp'), LIBS=[domain, base,
	zmq, jsoncpp, 'pthread', 'rt'])

Return('prog')

